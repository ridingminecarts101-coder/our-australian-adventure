# Python bytecode from the tools directory
__pycache__/
*.pyc

# Installed, not authored
node_modules/

# Staged copy of the app, rebuilt by tools/stage.mjs on every sync
www/

# Native build output and machine-local paths
android/.gradle/
android/.kotlin/
android/build/
android/app/build/
android/local.properties
android/capacitor-cordova-android-plugins/
ios/App/Pods/
ios/App/build/
ios/capacitor-cordova-ios-plugins/
DerivedData/

# Signing material. These sign releases as you; they never go in a public repo.
*.jks
*.keystore
keystore.properties
android/keystore/
dist/
android-sim.html
android-sim-nokey.html

# Google Play service account. This is a key to your store account - it can
# upload and publish releases as you. Same category as the keystore.
play-service-account.json
*-service-account.json
